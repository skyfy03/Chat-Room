import ZoneHandler from "./ZoneHandler.js";

export default class CraftSpellZoneHandler extends ZoneHandler {
    constructor(scene) {
        super(scene);

        this.CraftSpellZone = true;



    }
}